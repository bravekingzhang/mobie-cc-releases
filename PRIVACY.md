# Mobie CC 隐私说明

生效日期：2026 年 8 月 15 日

Mobie CC 由老码小张提供。联系邮箱：`1595819400@qq.com`。

## 数据处理概览

Mobie CC 的 Agent 编排、Vault 检索索引、工具执行、对话、候选和长期记忆管理均在当前 Obsidian 设备内完成。插件不提供开发者运营的模型中转服务，不包含广告追踪，也不收集 Vault 内容或对话行为分析。启用“匿名活跃设备统计”时，插件每天最多向 TelemetryDeck 发送一次最小化的日活事件，具体字段见下文。

模型推理不是本地完成的。用户发起 Agent 请求时，插件会直接连接用户选择的第三方模型服务。对话、压缩摘要、已确认的分层记忆、用户选择的图片/PDF/Canvas，以及 Agent 按需取得的笔记片段、网页或外部工具结果，可能被发送到该模型服务。对应服务如何处理数据，由用户与该服务商之间的条款和隐私政策决定。

## 本地保存的数据

插件可能在 Vault 或 Obsidian 插件存储中保存：

- 插件设置、对话、可撤销上下文摘要、运行检查点、知识候选和记忆候选；
- 用于 Vault 检索的本地索引与笔记片段；
- `.mobie-cc/context/` 中由用户确认的稳定记忆、Projects 和按日期保存的 Daily Log；
- `.mobie-cc/skills/` 中的 `SKILL.md` 与用户添加的文本参考资料；
- 插件目录中有数量上限的工具审计预览和超长工具结果附件；
- Obsidian `SecretStorage` 中的模型 API Key、MCP Bearer Token、随机安装 ID、激活状态，以及与安装码无关的匿名统计随机种子和最近成功发送日期。

API Key 不写入普通 `data.json`。用户可以通过删除对话、候选、索引、`.mobie-cc/context/` 或插件数据来移除对应本地内容；卸载插件前应自行决定是否保留备份。

## 网络访问

插件只在相关功能被使用时发起网络请求：

- **模型服务**：向用户配置的 API Base URL 发送模型请求，用于生成回答和结构化工具调用；
- **公开网页读取**：当 Agent 调用网页工具时，访问公开 HTTPS 文本 URL；用户可以配置禁止访问的域名；
- **远程 MCP**：仅当用户添加、启用并在当前会话选择公开 HTTPS MCP 服务时，插件进行工具发现或调用。发送的数据由具体工具参数决定，返回结果也可能继续进入模型上下文；
- **匿名日活统计**：启用后每天最多请求一次 TelemetryDeck。请求正文只包含 `MobieCC.App.dailyActive` 事件名、按 UTC 日期轮换的 SHA-256 匿名标识、插件版本和 Android/iOS/Desktop 平台。不会发送安装码、激活码、Vault、笔记、对话、模型、API Key、MCP 地址或错误正文。匿名标识跨日期不可直接关联；服务商仍可能按其隐私政策处理连接请求所必需的网络元数据；
- **Obsidian/GitHub 分发**：Obsidian 可能从公开 GitHub Release 下载插件更新，这由 Obsidian 和 GitHub 的服务完成。

插件不会主动访问本机、私网、HTTP 地址或用户配置的禁止域名。由于第三方模型可能建议访问某个 URL，用户应避免在问题中提供不希望发送给模型或网页服务的敏感信息。

## 试用、安装码与购买记录

插件在本机生成随机安装 ID，并派生为可复制的安装码。它不读取 IMEI、Android ID、设备序列号或其他硬件标识。激活码在本机离线验证，插件不会把安装码自动上传到授权服务器。

用户购买或申请激活码时，会主动通过联系方式向老码小张提供安装码及必要的订单、支付或联系信息。这些信息仅用于签发授权、核对购买和售后支持，不用于广告画像。除法律、支付争议或售后记录确有需要外，用户可以通过 `1595819400@qq.com` 请求查询、更正或删除所提供的信息。

## 数据分享

除用户主动配置并使用的模型服务、用户要求读取的网页服务、匿名日活统计服务，以及完成购买所使用的支付/通信渠道外，老码小张不会主动出售或共享用户的 Vault 内容、对话或本地索引。插件没有开发者运营的模型中转或遥测服务器。

## 用户选择

- 隐私模式只发送当前问题与当前对话，不读取或发送 Vault、附件、Skills、MCP、网页、候选或长期记忆；
- “不允许读取的目录”“允许写入的目录”和“禁止访问的网页域名”用于限制工具执行范围；
- 本地读取、网络读取、界面导航、候选创建和外部工具可以分别设为始终允许、每会话询问或禁止；
- 用户可以在设置页关闭“匿名活跃设备统计”；关闭后插件不会发起日活统计请求；
- 未激活或授权到期不会锁住用户的笔记、已有对话、候选或长期记忆；
- 用户可以停止使用、禁用或卸载插件，并自行删除本地插件数据。

## 变更

隐私说明发生实质变化时，将在公开分发仓库和版本说明中更新生效日期与变化内容。

## 联系方式

隐私、授权和支持问题：老码小张 <1595819400@qq.com>

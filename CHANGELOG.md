# Mobie CC 版本说明

## 未发布

## 0.3.11 — 2026-08-16

- 内置「卡兹克公众号长文写作」Skill（`khazix-writer`，来自数字生命卡兹克开源的 khazix-skills，MIT 协议）：含完整写作方法论与内容方法论、风格示例两份参考资料；默认关闭，需在设置 → Skills 中手动开启。
- Skills 管理的启用开关改为 iOS 风格滑动开关，带过渡动画、按压反馈和 `role="switch"` 无障碍语义。

## 0.3.10 — 2026-08-16

- 改进移动端授权弹框的键盘适配：不再依赖 `visualViewport` 检测（部分 Android WebView 键盘弹出时高度不变），触摸设备上输入框聚焦即视为键盘打开；弹框在可见区域内垂直居中并轻微上移，不再顶到屏幕最上方，状态卡片自动收起，输入框与操作按钮保持可见。
- 工作流选择列表的副标题改为完整换行显示（配合 `overflow-wrap` 防止溢出），不再做行数截断，长描述全文可见。

## 0.3.9 — 2026-08-16

- 修复授权弹框在移动端被键盘遮挡的问题：键盘弹出时弹框直接固定在可视区域上方并补偿 iOS 视口滚动偏移，隐藏标题和状态卡片，输入框始终可见可粘贴。
- 工作流选择列表的副标题增加两行截断，超长描述不再溢出行布局。
- 对话内容中的链接改为统一拦截：`http(s)` 与 `obsidian://` 链接交给系统浏览器打开，Vault 内相对路径用 `openLinkText` 正确解析，修复 iOS 点击链接黑屏。

## 0.3.8 — 2026-08-15

- 增加默认关闭的“技术公众号与博客写作”内置 Skill；启用后按真实问题、机制、仓库代码、可复现实验、边界说明和发布前自查协作写作，缺少证据时明确索取而不编造。
- 新增内置 Skill 默认启用策略：保持原有三个工作流启用，新加入的重型写作规范只有用户主动启用后才进入 Agent 可用能力。

## 0.3.7 — 2026-08-15

- 移除切后台时主动终止 Agent 的逻辑；系统仍允许 Obsidian 运行时任务会尽力继续。
- 增加持久化工具运行账本：执行前记录 `started`，执行后保存结果；恢复任务会直接复用已完成结果，不再重复调用。
- 外部 MCP 操作在中断时结果未知会默认阻止自动重试；知识候选和长期记忆候选增加内容级去重，降低恢复竞态导致的重复候选。
- “从检查点继续”不再插入一条伪造的继续消息，也不会重复保存原始用户问题。
- 移动端索引升级为有硬上限的 v2 格式，不再读取会放大内存的 v1 缓存；限制单文件、词项、片段与序列化体积，优先保留最近修改的笔记，并取消移动端常驻倒排表。
- 索引从插件启动关键路径移到工作区就绪后加载；使用临时文件替换缓存，并在索引前持久记录操作进度。若 iOS/Android 在索引中终止进程，下次启动会丢弃可疑缓存，避免持续闪退。
- 增加本地诊断环形日志和 JSON 导出，覆盖启动、索引、模型连接、授权、Agent、工具与候选关键节点；日志不写入笔记/聊天正文、API Key 或激活码，并提示 iOS 用户同时提供系统 `.ips`。
- 工具完整结果改存入有数量上限的独立运行附件，检查点只保留受限预览，成功完成后移除恢复账本，避免多会话长期使用使 `data.json` 持续膨胀。

## 0.3.6 — 2026-08-15

- 增加 TelemetryDeck 匿名日活统计框架：设备匿名标识按 UTC 日期轮换，每天最多发送一次，设置页可以关闭；请求只包含事件名、插件版本和 Android/iOS/Desktop 平台，不复用安装码或激活信息。
- 统计服务未填写 Namespace 或 App ID 时保持完全静默，不发起网络请求；当前 App 配置已通过 `isTestMode` 事件验证。README 不增加统计说明，具体数据边界只在设置页和隐私说明中展示。

## 0.3.5 — 2026-08-15

这一轮把 Mobie CC 的 Agent 底座升级为适合长任务和可扩展工作流的端侧运行时：

- 按模型上下文窗口自动压缩较早对话，保留原始消息和可撤销摘要；每轮、工具结果和中断状态都会形成持久检查点；
- 支持多个模型配置、会话级选择、能力元数据，以及只对超时、限流和服务端故障生效的有界回退；
- 工具增加严格参数预检、风险权限、最多四个独立只读调用并发、结果附件和本地审计；
- 增加标准 `SKILL.md`、内置公众号/周报/知识综合工作流、按需加载、会话开关和 Slash 命令；
- 在原有四类长期记忆外增加 Projects 与按日期保存的 Daily Log，所有新记忆继续走候选审核；
- 增加远程 HTTPS MCP 的工具发现、SecretStorage 凭据、命名空间、允许列表和会话级授权；
- 增加会话分叉、Obsidian URI、编辑器选区、当前笔记命令，以及图片、PDF、Canvas 附件和移动请求预算；
- 插件状态升级到 Schema v4，旧模型配置会迁移为第一个模型 Profile；TypeScript、单元测试、生产构建和移动依赖审计已通过；Android 已完成覆盖安装、初始化、URI 唤起与输入区冒烟验证，完整交互和 iOS/iPadOS 仍待回归后发布。

## 0.3.4 — 2026-08-14

这一版修复手机软键盘遮挡授权输入区的问题：

- 授权弹窗改用移动端真实可视高度，在 Android 和 iOS 键盘弹起时停在键盘上方；
- 输入激活码时自动收起上方状态说明，把输入框与“验证并激活”按钮留在可视区域；
- 弹窗内容支持内部滚动，键盘收起后恢复完整的授权状态、安装码与隐私说明。

## 0.3.3 — 2026-08-14

这一版收口闭源插件的用户安装与功能状态文档：

- 重写手机安装指南，提供 BRAT 推荐安装、自动更新、ZIP 手动安装、升级、排障和卸载步骤；
- 增加 Android 实际 Vault 路径示例和 BRAT 一键添加 URI；
- 将 TODO 重构为纯功能清单，仅保留已完成与待完成功能，删除内部优先级、工作量和评审记录；
- 增加只在本机运行的激活码签发台，支持永久、一个月和自定义期限授权，签发后自动验签、一键复制、搜索历史与导出 CSV；
- 继续保持闭源分发，公开仓库只提供用户文档、版本元数据和 Release 编译产物。

## 0.3.2 — 2026-08-14

这一版处理 Obsidian Community directory 的首轮自动审查反馈：

- manifest 描述移除目录上下文中冗余的 “Obsidian” 字样；
- 公开 README 增加完整英文产品、安装、隐私、授权和闭源说明；
- 公开仓库链接从组织地址切换到与提交账号一致的 `bravekingzhang/mobie-cc-releases`；
- 保留老码小张专有商业许可证；它不是标准开源许可证，因此目录可能继续显示非阻断的许可证识别 Warning。
- 后续自动审查确认目录暂不支持闭源插件；决定保持源码私有，停止市场上架，并将正式安装渠道调整为 BRAT 与手动 ZIP。

## 0.3.1 — 2026-08-14

这一版加入适合移动端 Obsidian 的离线授权闭环，并把官方市场与闭源发布边界写入项目文档。

### 试用与激活

- 首次加载提供 24 小时完整 Agent 试用；试用结束后只暂停新的 Agent 轮次，不锁住笔记、历史对话、知识候选、长期记忆或设置；
- 使用 Ed25519 非对称签名，客户端只内嵌公钥，私钥只存在于被 Git 忽略的离线签发文件；
- 每台设备在 Obsidian SecretStorage 生成随机安装 ID，再派生为便于复制的五段安装码，不读取 IMEI、Android ID 或硬件序列号；
- 支持永久授权和指定到期日授权；每次 Agent 执行前重新检查签名、安装码与有效期；
- 主聊天输入区显示试用/锁定状态，锁定后发送会直接打开激活页；设置页可复制安装码、输入或更换激活码；
- 增加本机时间回拨检测，但明确记录纯客户端授权仍可被修改代码绕过，不把它描述成不可破解 DRM。
- 正式披露永久激活码 99 元、一个月激活码 19.9 元，购买与支持联系老码小张；

### 签发与发布工程

- 新增 `license:init`、`license:sign` 和 `license:verify` 命令，以及独立的密钥保管与签发手册；
- Mobie 使用独立密钥，不复用参考项目的产品密钥；
- 新增 `release:stage`，生产构建后只把 `main.js`、`manifest.json`、`styles.css` 放入 `dist/release`，并检查版本一致性、source map 和私钥泄漏；
- `release:stage` 同时生成带 `mobie-cc/` 顶层目录的手动安装 ZIP，避免用户放错插件层级；
- 手动安装 ZIP 同时生成独立 SHA-256 文件，方便分发时核对完整性；
- 增加面向用户的两种安装教程，以及面向开发者的 GitHub Release 与 Community Plugins 发布清单；
- 接入 Obsidian 官方 ESLint 规则并清零阻断错误；修正卸载时强制关闭聊天叶片、模型响应 `any`、异步 UI 回调和直接删除撤销文件等审查问题；
- 按当前目录机制区分自动审查与人工审查：自动审查通过后即可发布，未人工审核提示由平台生成；闭源代码仍需逐案处理；
- 增加老码小张专有商业许可证、隐私说明、第三方 MIT 组件声明，以及严格白名单的公开分发仓库暂存命令；
- 首个市场候选版最低支持版本调整为已完成 Android 真机回归的 Obsidian `1.13.6`；
- README 补充 Community Plugins 规则：用户安装的是三个构建产物，官方审核仍需源码访问；闭源需披露并逐案审核，不能通过混淆隐藏用途；
- 明确不采用隐藏 Prompt 阻止逆向，因为源码字符串不会成为分析 Agent 的高优先级指令，也不能构成实际安全边界。

### 验证

- 授权单测覆盖有效永久码、设备不匹配、订阅到期、安装码格式和 24 小时试用到期；
- TypeScript、单元测试、生产构建和移动依赖审计继续作为发布门禁；
- Android PHZ110 已完成覆盖安装、旧数据保留、重启加载、试用状态条，以及与本机安装码绑定的永久激活码验证；SecretStorage 再次重启持久化、到期/错机码与 iOS/iPadOS 仍待回归。

## 0.2.1 — 2026-08-14

这是第一版完整的移动知识 Agent 核心闭环候选版本。产品主张从“在手机上调用模型”调整为：让 Obsidian 里积累过的笔记持续参与今天的工作。

### 更贴近 Obsidian 的工作方式

- 主界面提供“从 Vault 找答案”“把这篇做得更好”“把成果留在 Vault”三个直接面向任务结果的入口；
- 当前笔记和手动引用统一放在输入框附近，不再提供功能重叠的顶部上下文入口；
- 支持 `@` 引用笔记或文件夹，多会话可新建、切换、重命名和删除；
- 知识候选可以编辑标题与正文、选择目标、预览 diff、确认写入并在安全条件满足时撤销。

### 移动端交互优化

- 输入框、发送按钮与工具栏重新对齐，缩小输入字号并优化多行输入；
- 避开 Obsidian 移动端浮动底栏和系统安全区；键盘弹出时不再把输入区顶到页面高处；
- 修复对话列表 Item 高度不足造成的标题与摘要重叠；
- 引用 Chip 紧贴输入区显示，设置页、审核页和主页面统一为面向用户价值的产品文案。

### Agent 工具能力

- 提供 12 个结构化工具，覆盖 Vault 搜索、目录枚举、单篇/批量读取、当前笔记、标题结构、Properties/标签/链接关系、打开笔记、历史对话召回、知识候选、长期记忆候选和公开网页读取；
- 以 Obsidian Vault、MetadataCache 和 Workspace API 映射 Claude Code/Codex 的核心任务能力，不在手机插件中暴露 Shell 或任意文件系统；
- 加入工具预算、Observation 上限、重复调用检测、请求总超时，以及 HTTP 429/5xx 的有限重试；
- 修复智谱 Anthropic/OpenAI 兼容接口的端点、模型名和空响应诊断，连接测试会真实探测 Function Calling。

### 越用越懂你的长期记忆

- 在 Vault 的 `.mobie-cc/context/` 初始化 `profile.md`、`style.md`、`work-mode.md` 和 `vault.md`；
- 每轮非私密对话加载一份有字符上限的冻结快照，让 Agent 延续用户目标、表达偏好、工作方式和 Vault 约定；
- `search_conversations` 可按需检索历史会话，不把完整历史常驻到每次模型上下文；
- 新偏好不会静默写入：Agent 先创建记忆候选，用户可在审核中心修改、拒绝或确认，写入后从下一轮生效；
- 设置页提供长期记忆编辑器，隐藏文件仍可在 Obsidian 内查看和修正。

### 检索、隐私与可靠性

- 新增版本化持久 BM25 索引，支持中文 bigram、标题/标题层级/标签加权和 Vault 文件事件增量更新；
- 可配置禁止读取目录、允许写入目录和禁止访问的网页域名；访问范围变化后自动清理并重建索引；
- 私密模式不读取长期记忆、不公布任何工具，并硬拒绝兼容模型自行返回的工具调用；
- 插件状态升级到 Schema v3，迁移旧单会话数据并校验设置、会话与候选；API Key 继续保存在 Obsidian SecretStorage。

### 升级说明

- 从旧版升级时直接覆盖 `main.js`、`manifest.json` 和 `styles.css`，不要删除插件目录中的 `data.json`；
- 长期记忆文件位于隐藏目录 `.mobie-cc/context/`，可从“设置 → Mobie CC → 你的长期上下文 → 查看与编辑”访问；
- 版本最低要求仍为 Obsidian `1.11.4`；Android PHZ110 / Obsidian `1.13.6` 已完成安装、启动、数据迁移、主界面和模型工具链路验证。

### 仍待验证

- Android 上知识候选与记忆候选的完整编辑、diff、应用、撤销真机回归；
- 大型 Vault 的 1k/10k/50k 固定数据集性能基线；
- 锁屏、系统回收、横竖屏、系统字号及 Android 返回手势；
- iOS 与 iPadOS 的完整安装和核心流程验证。
